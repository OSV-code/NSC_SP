"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { getSupabase } from "@/lib/supabase";
export function AdminAuth() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); const [resetMode, setResetMode] = useState(false); const router = useRouter(); async function login(event: React.FormEvent) { event.preventDefault(); const supabase = getSupabase(); const { data, error } = await supabase.auth.signInWithPassword({ email, password }); if (error || !data.user) return setMessage(error?.message ?? "Login failed."); const { data: profile } = await supabase.from("profiles").select("role").eq("id", data.user.id).single(); if (profile?.role !== "admin") { await supabase.auth.signOut(); return setMessage("This account does not have State President access."); } router.push("/admin/dashboard"); } async function requestReset(event: React.FormEvent) { event.preventDefault(); if (!email) return setMessage("Enter your email address first."); const { error } = await getSupabase().auth.resetPasswordForEmail(email, { redirectTo: `${window.location.origin}/auth/reset-password` }); setMessage(error?.message ?? "Check your email for a password reset link."); } if (resetMode) return <form onSubmit={requestReset} className="space-y-4"><label className="block text-sm font-bold">Email<input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="mt-1.5 w-full rounded-md border border-[#c4ccc5] p-2.5" /></label><button className="rounded-md bg-[#174c3e] px-5 py-2.5 font-bold text-white">Send reset link</button><button type="button" onClick={() => { setResetMode(false); setMessage(""); }} className="block text-sm font-bold text-[#174c3e] underline">Back to sign in</button>{message && <p className="text-sm text-[#405249]">{message}</p>}</form>; return <form onSubmit={login} className="space-y-4"><label className="block text-sm font-bold">Email<input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="mt-1.5 w-full rounded-md border border-[#c4ccc5] p-2.5" /></label><label className="block text-sm font-bold">Password<input required type="password" value={password} onChange={(event) => setPassword(event.target.value)} className="mt-1.5 w-full rounded-md border border-[#c4ccc5] p-2.5" /></label><button className="rounded-md bg-[#174c3e] px-5 py-2.5 font-bold text-white">Sign in</button><button type="button" onClick={() => { setResetMode(true); setMessage(""); }} className="block text-sm font-bold text-[#174c3e] underline">Forgot password?</button>{message && <p className="text-sm text-red-700">{message}</p>}</form>; }